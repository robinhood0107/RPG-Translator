# RPG-Translator

RPG Maker MV/MZ pre-caching translator.

Detailed local specification lives in local-only `SPEC.md`.

`SPEC.md`, `spec_ko.md`, `AGENTS.md`, `ROADMAP.md`, `dontupload/`, local model files, and local test artifacts are intentionally not committed.
